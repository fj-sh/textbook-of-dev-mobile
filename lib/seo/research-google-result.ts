/**
 * キーワードの検索結果のタイトル、見出し、本文の情報を整理して返す。
 * 記事を書く前のリサーチとして使う。
 * @param {string} keyword
 * @param {number} paginationCount
 */
function collectGoogleResult(keyword: string, paginationCount: number): string {
  let contents = ''

  return contents
}

export {}
